# Android 0.2.9 behaviour note

The gameplay rules still reuse the iOS project where they are reliable (cargo/card-first safety and magenta filter anchors), but Android no longer copies the slow per-tap validation path introduced in alpha10. Android-specific screenshot/display mapping and transition state checks remain necessary.
